spring:
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: prod
      label: dev

  application:
    name: movie
server:
  port: 2001
eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:1001/eureka/,http://peer2:1002/eureka/